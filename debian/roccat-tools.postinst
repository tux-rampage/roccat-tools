#!/bin/sh

set -e

GROUP_NAME=roccat

case "$1" in
    configure)

	if ! getent group "$GROUP_NAME" > /dev/null 2>&1 ; then
	    addgroup --system "$GROUP_NAME" --quiet
	fi
	chown root:roccat /var/lib/roccat || true
	chmod 2770 /var/lib/roccat || true
    ;;
esac

#DEBHELPER#

